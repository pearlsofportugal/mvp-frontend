/**
 * Generated by orval v8.5.1 🍺
 * Do not edit manually.
 * MVP-Scraper
 * Real Estate Scraper Backend API — scrape, enrich, and serve property listings.
 * OpenAPI spec version: 1.0.0
 */
import { HttpClient } from '@angular/common/http';

import { Injectable, inject } from '@angular/core';

import type {
  ApiResponseDuplicatesResponse,
  ApiResponseListingDetailRead,
  ApiResponseListingSearchResponse,
  ApiResponseListingStats,
  ApiResponseNoneType,
  ApiResponsePaginatedResponse,
  DetectDuplicatesParams,
  ListListingsParams,
  ListingCreate,
  ListingStatsParams,
  ListingUpdate,
  SearchListingsParams,
} from '../../model';

import { customFetch } from '../../custom-fetch';

@Injectable({ providedIn: 'root' })
export class ListingsService {
  private readonly http = inject(HttpClient);
  /**
   * List listings with filtering, sorting, and pagination.
   * @summary List Listings
   */
  listListings<TData = ApiResponsePaginatedResponse>(params?: ListListingsParams) {
    return customFetch<TData>(
      {
        url: `/api/v1/listings`,
        method: 'GET',
        params: (() => {
          const requiredNullableParamKeys = new Set<string>([]);
          const filteredParams = {} as Record<
            string,
            string | number | boolean | null | Array<string | number | boolean>
          >;
          for (const [key, value] of Object.entries(params ?? {})) {
            if (Array.isArray(value)) {
              const filtered = value.filter(
                (item) =>
                  item != null &&
                  (typeof item === 'string' ||
                    typeof item === 'number' ||
                    typeof item === 'boolean'),
              ) as Array<string | number | boolean>;
              if (filtered.length) {
                filteredParams[key] = filtered;
              }
            } else if (value === null && requiredNullableParamKeys.has(key)) {
              filteredParams[key] = value;
            } else if (
              value != null &&
              (typeof value === 'string' || typeof value === 'number' || typeof value === 'boolean')
            ) {
              filteredParams[key] = value as string | number | boolean;
            }
          }
          return filteredParams as unknown as Record<
            string,
            string | number | boolean | Array<string | number | boolean>
          >;
        })(),
      },
      this.http,
    );
  }
  /**
   * Create a new listing manually.
   * @summary Create Listing
   */
  createListing<TData = ApiResponseListingDetailRead>(listingCreate: ListingCreate) {
    return customFetch<TData>(
      {
        url: `/api/v1/listings`,
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        data: listingCreate,
      },
      this.http,
    );
  }
  /**
 * Lightweight search endpoint for the listing selector UI.

Returns thumbnail_url (first media asset) and is_enriched flag.
 * @summary Search Listings
 */
  searchListings<TData = ApiResponseListingSearchResponse>(params?: SearchListingsParams) {
    return customFetch<TData>(
      {
        url: `/api/v1/listings/search`,
        method: 'GET',
        params: (() => {
          const requiredNullableParamKeys = new Set<string>([]);
          const filteredParams = {} as Record<
            string,
            string | number | boolean | null | Array<string | number | boolean>
          >;
          for (const [key, value] of Object.entries(params ?? {})) {
            if (Array.isArray(value)) {
              const filtered = value.filter(
                (item) =>
                  item != null &&
                  (typeof item === 'string' ||
                    typeof item === 'number' ||
                    typeof item === 'boolean'),
              ) as Array<string | number | boolean>;
              if (filtered.length) {
                filteredParams[key] = filtered;
              }
            } else if (value === null && requiredNullableParamKeys.has(key)) {
              filteredParams[key] = value;
            } else if (
              value != null &&
              (typeof value === 'string' || typeof value === 'number' || typeof value === 'boolean')
            ) {
              filteredParams[key] = value as string | number | boolean;
            }
          }
          return filteredParams as unknown as Record<
            string,
            string | number | boolean | Array<string | number | boolean>
          >;
        })(),
      },
      this.http,
    );
  }
  /**
   * Aggregated listing statistics.
   * @summary Listing Stats
   */
  listingStats<TData = ApiResponseListingStats>(params?: ListingStatsParams) {
    return customFetch<TData>(
      {
        url: `/api/v1/listings/stats`,
        method: 'GET',
        params: (() => {
          const requiredNullableParamKeys = new Set<string>([]);
          const filteredParams = {} as Record<
            string,
            string | number | boolean | null | Array<string | number | boolean>
          >;
          for (const [key, value] of Object.entries(params ?? {})) {
            if (Array.isArray(value)) {
              const filtered = value.filter(
                (item) =>
                  item != null &&
                  (typeof item === 'string' ||
                    typeof item === 'number' ||
                    typeof item === 'boolean'),
              ) as Array<string | number | boolean>;
              if (filtered.length) {
                filteredParams[key] = filtered;
              }
            } else if (value === null && requiredNullableParamKeys.has(key)) {
              filteredParams[key] = value;
            } else if (
              value != null &&
              (typeof value === 'string' || typeof value === 'number' || typeof value === 'boolean')
            ) {
              filteredParams[key] = value as string | number | boolean;
            }
          }
          return filteredParams as unknown as Record<
            string,
            string | number | boolean | Array<string | number | boolean>
          >;
        })(),
      },
      this.http,
    );
  }
  /**
   * Detect duplicate listings by source_url.
   * @summary Detect Duplicates
   */
  detectDuplicates<TData = ApiResponseDuplicatesResponse>(params?: DetectDuplicatesParams) {
    return customFetch<TData>(
      {
        url: `/api/v1/listings/duplicates`,
        method: 'GET',
        params: (() => {
          const requiredNullableParamKeys = new Set<string>([]);
          const filteredParams = {} as Record<
            string,
            string | number | boolean | null | Array<string | number | boolean>
          >;
          for (const [key, value] of Object.entries(params ?? {})) {
            if (Array.isArray(value)) {
              const filtered = value.filter(
                (item) =>
                  item != null &&
                  (typeof item === 'string' ||
                    typeof item === 'number' ||
                    typeof item === 'boolean'),
              ) as Array<string | number | boolean>;
              if (filtered.length) {
                filteredParams[key] = filtered;
              }
            } else if (value === null && requiredNullableParamKeys.has(key)) {
              filteredParams[key] = value;
            } else if (
              value != null &&
              (typeof value === 'string' || typeof value === 'number' || typeof value === 'boolean')
            ) {
              filteredParams[key] = value as string | number | boolean;
            }
          }
          return filteredParams as unknown as Record<
            string,
            string | number | boolean | Array<string | number | boolean>
          >;
        })(),
      },
      this.http,
    );
  }
  /**
   * Get a single listing by ID.
   * @summary Get Listing
   */
  getListing<TData = ApiResponseListingDetailRead>(listingId: string) {
    return customFetch<TData>({ url: `/api/v1/listings/${listingId}`, method: 'GET' }, this.http);
  }
  /**
   * Partially update a listing.
   * @summary Update Listing
   */
  updateListing<TData = ApiResponseListingDetailRead>(
    listingId: string,
    listingUpdate: ListingUpdate,
  ) {
    return customFetch<TData>(
      {
        url: `/api/v1/listings/${listingId}`,
        method: 'PATCH',
        headers: { 'Content-Type': 'application/json' },
        data: listingUpdate,
      },
      this.http,
    );
  }
  /**
   * Delete a listing (hard delete — cascades to media_assets and price_history).
   * @summary Delete Listing
   */
  deleteListing<TData = ApiResponseNoneType>(listingId: string) {
    return customFetch<TData>(
      { url: `/api/v1/listings/${listingId}`, method: 'DELETE' },
      this.http,
    );
  }
}
