/**
 * Generated by orval v8.5.1 🍺
 * Do not edit manually.
 * MVP-Scraper
 * Real Estate Scraper Backend API — scrape, enrich, and serve property listings.
 * OpenAPI spec version: 1.0.0
 */
import { HttpClient } from '@angular/common/http';

import { Injectable, inject } from '@angular/core';

import type {
  ApiResponseListSiteConfigRead,
  ApiResponseNoneType,
  ApiResponseSiteConfigPreviewResponse,
  ApiResponseSiteConfigRead,
  ApiResponseSiteConfigSuggestResponse,
  DeleteSiteParams,
  ListSitesParams,
  SiteConfigCreate,
  SiteConfigPreviewRequest,
  SiteConfigSuggestRequest,
  SiteConfigUpdate,
} from '../../model';

import { customFetch } from '../../custom-fetch';

@Injectable({ providedIn: 'root' })
export class SitesService {
  private readonly http = inject(HttpClient);
  /**
   * Suggest likely selectors for a listing detail page before saving a site config.
   * @summary Suggest Site Selectors
   */
  suggestSiteSelectors<TData = ApiResponseSiteConfigSuggestResponse>(
    siteConfigSuggestRequest: SiteConfigSuggestRequest,
  ) {
    return customFetch<TData>(
      {
        url: `/api/v1/sites/preview/selector-suggestions`,
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        data: siteConfigSuggestRequest,
      },
      this.http,
    );
  }
  /**
   * Preview live matches for a single selector against a detail page.
   * @summary Preview Site Selector
   */
  previewSiteSelector<TData = ApiResponseSiteConfigPreviewResponse>(
    siteConfigPreviewRequest: SiteConfigPreviewRequest,
  ) {
    return customFetch<TData>(
      {
        url: `/api/v1/sites/preview/selector`,
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        data: siteConfigPreviewRequest,
      },
      this.http,
    );
  }
  /**
   * List all configured scraping sites.
   * @summary List Sites
   */
  listSites<TData = ApiResponseListSiteConfigRead>(params?: ListSitesParams) {
    return customFetch<TData>(
      {
        url: `/api/v1/sites`,
        method: 'GET',
        params: (() => {
          const requiredNullableParamKeys = new Set<string>([]);
          const filteredParams = {} as Record<
            string,
            string | number | boolean | null | Array<string | number | boolean>
          >;
          for (const [key, value] of Object.entries(params ?? {})) {
            if (Array.isArray(value)) {
              const filtered = value.filter(
                (item) =>
                  item != null &&
                  (typeof item === 'string' ||
                    typeof item === 'number' ||
                    typeof item === 'boolean'),
              ) as Array<string | number | boolean>;
              if (filtered.length) {
                filteredParams[key] = filtered;
              }
            } else if (value === null && requiredNullableParamKeys.has(key)) {
              filteredParams[key] = value;
            } else if (
              value != null &&
              (typeof value === 'string' || typeof value === 'number' || typeof value === 'boolean')
            ) {
              filteredParams[key] = value as string | number | boolean;
            }
          }
          return filteredParams as unknown as Record<
            string,
            string | number | boolean | Array<string | number | boolean>
          >;
        })(),
      },
      this.http,
    );
  }
  /**
 * Add a new site configuration.

If a deactivated site with the same key exists, it will be reactivated and updated.
 * @summary Create Site
 */
  createSite<TData = ApiResponseSiteConfigRead>(siteConfigCreate: SiteConfigCreate) {
    return customFetch<TData>(
      {
        url: `/api/v1/sites`,
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        data: siteConfigCreate,
      },
      this.http,
    );
  }
  /**
   * Get a site configuration by key.
   * @summary Get Site
   */
  getSite<TData = ApiResponseSiteConfigRead>(key: string) {
    return customFetch<TData>({ url: `/api/v1/sites/${key}`, method: 'GET' }, this.http);
  }
  /**
   * Update a site configuration.
   * @summary Update Site
   */
  updateSite<TData = ApiResponseSiteConfigRead>(key: string, siteConfigUpdate: SiteConfigUpdate) {
    return customFetch<TData>(
      {
        url: `/api/v1/sites/${key}`,
        method: 'PATCH',
        headers: { 'Content-Type': 'application/json' },
        data: siteConfigUpdate,
      },
      this.http,
    );
  }
  /**
 * Delete a site configuration.

By default, performs a soft delete (deactivates the site).
Use permanent=true to permanently delete the record.
 * @summary Delete Site
 */
  deleteSite<TData = ApiResponseNoneType>(key: string, params?: DeleteSiteParams) {
    return customFetch<TData>(
      {
        url: `/api/v1/sites/${key}`,
        method: 'DELETE',
        params: (() => {
          const requiredNullableParamKeys = new Set<string>([]);
          const filteredParams = {} as Record<
            string,
            string | number | boolean | null | Array<string | number | boolean>
          >;
          for (const [key, value] of Object.entries(params ?? {})) {
            if (Array.isArray(value)) {
              const filtered = value.filter(
                (item) =>
                  item != null &&
                  (typeof item === 'string' ||
                    typeof item === 'number' ||
                    typeof item === 'boolean'),
              ) as Array<string | number | boolean>;
              if (filtered.length) {
                filteredParams[key] = filtered;
              }
            } else if (value === null && requiredNullableParamKeys.has(key)) {
              filteredParams[key] = value;
            } else if (
              value != null &&
              (typeof value === 'string' || typeof value === 'number' || typeof value === 'boolean')
            ) {
              filteredParams[key] = value as string | number | boolean;
            }
          }
          return filteredParams as unknown as Record<
            string,
            string | number | boolean | Array<string | number | boolean>
          >;
        })(),
      },
      this.http,
    );
  }
}
